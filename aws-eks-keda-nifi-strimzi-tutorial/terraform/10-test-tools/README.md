# 10 - Test Tools

This folder creates a long-running toolbox pod and read-focused Kubernetes RBAC
for the `tutorial-testers` group used by the SSM EC2 runner. The runner can
inspect resources and execute approved commands inside existing pods.
