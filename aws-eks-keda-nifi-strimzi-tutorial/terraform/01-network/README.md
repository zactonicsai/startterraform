# 01 - Network

This folder creates the fenced network area: VPC, subnets, internet gateway,
NAT Gateway, and route tables. EKS worker nodes later use the private subnets.

```bash
terraform init
terraform plan
terraform apply
```
